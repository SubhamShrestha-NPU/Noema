import { TextEmbeddingPipelineWrapper, EmbeddingResult, EmbeddingResults, TextEmbeddingConfig } from "../addon.js";
export declare class TextEmbeddingPipeline {
    modelPath: string;
    device: string;
    config: TextEmbeddingConfig;
    ovProperties: object;
    pipeline: TextEmbeddingPipelineWrapper | null;
    isInitialized: boolean;
    constructor(modelPath: string, device: string, config: TextEmbeddingConfig, ovProperties?: object);
    init(): Promise<void>;
    embedDocumentsSync(texts: string[]): EmbeddingResults;
    embedQuerySync(text: string): EmbeddingResult;
    embedDocuments(documents: string[]): Promise<EmbeddingResults>;
    embedQuery(text: string): Promise<EmbeddingResult>;
}
